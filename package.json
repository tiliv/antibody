{
  "comment": "Thin relay to the journal engine (mounted at journal/). The build logic lives in journal/bin; add site-specific scripts here as needed.",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "stats": "journal/bin/stats.sh",
    "sync": "journal/bin/sync.sh",
    "build": "journal/bin/build.sh",
    "serve": "journal/bin/serve.sh",
    "prebuild": "journal/bin/stats.sh"
  }
}
